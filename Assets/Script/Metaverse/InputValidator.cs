using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class InputValidator
{
    // Start is called before the first frame update
    public static bool voidStart()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
